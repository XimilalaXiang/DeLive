This directory is reserved for bundled local ASR runtimes.

Current planned layout:

- `local-runtimes/whisper_cpp/whisper-server(.exe)`

The app also supports importing runtime binaries and models into the user data directory at runtime.
